{
  "name": "eslint-config-repro",
  "version": "0.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && tsc --project tsconfig.build.json",
    "lint": "eslint --flag unstable_ts_config"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "jiti": "^1.21.6",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "eslint-plugin-react-refresh": "^0.4.11",
    "typescript-eslint": "^8.5.0"
  }
}
